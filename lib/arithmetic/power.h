#include <stdio.h>
#include <math.h>

/**
 * Power.
 * @params {double} a - Value a.
 * @params {double} b - Value b.
 * @returns Returns a total whose value is the value of a exponentiated by b, i.e. raised to the power b.
 */
double power(double a, double b) {
	return pow(a, b);
}

