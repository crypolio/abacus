#include <stdio.h>

/**
 * Modulo.
 * @params {double} a - Value a.
 * @params {double} b - Value b.
 * @returns Returns a double whose value is the value of a modulo b.
 */
double modulo(double a, double b) {
	int x = a;
	int y = b;

	return x % y;
}

