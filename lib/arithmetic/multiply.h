#include <stdio.h>

/**
 * Multiply.
 * @params {double} a - Value a.
 * @params {double} b - Value b.
 * @returns Returns a double whose value is the value of a multiplied by b.
 */
double multiply(double a, double b) {
	return a * b;
}

