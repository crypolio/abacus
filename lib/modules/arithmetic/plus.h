#include <stdio.h>

/**
 * Plus.
 * @params {double} a - Value a.
 * @params {double} b - Value b.
 * @returns Returns a double whose value is the value of a plus b.
 */
double plus(double a, double b) {
	return a + b;
}

