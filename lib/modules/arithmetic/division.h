#include <stdio.h>

/**
 * Division.
 * @params {double} a - Value a.
 * @params {double} b - Value b.
 * @returns Returns a double whose value is the value of a divided by b.
 */
double division(double a, double b) {
	return a / b;
}

