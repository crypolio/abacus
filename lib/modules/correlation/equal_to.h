#include <stdio.h>

/**
 * Is equal to.
 * @params {double} a - Value a.
 * @params {double} b - Value b.
 * @returns Returns 1 if the value of a is equal to the value of b, otherwise returns 0.
 */
int equal_to(double a, double b) {
	return a ==  b ? 1 : 0;
}

